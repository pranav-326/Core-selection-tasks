# Copy this to `.env.local` or use your own env mechanism.
# This demonstrates overriding API_BASE for production deployments.

API_BASE=https://api.your-production-domain.com
# In this project you can update `js/config.js` or use an automated build step
# to replace the value of API_BASE with environment-specific values.
